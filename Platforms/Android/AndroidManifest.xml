<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="alpha">
	<!-- Разрешения -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.VIBRATE" />
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<application android:icon="@mipmap/icons_medicines" 
				 android:label="Medicine Tracker"
				 android:enableOnBackInvokedCallback="true">>
		<!-- Только BroadcastReceiver, никакого ForegroundService -->
		<receiver android:name=".Platforms.Android.NotificationPublisher" android:enabled="true" android:exported="false">
			<intent-filter>
				<action android:name="android.intent.action.BOOT_COMPLETED" />
				<action android:name="ACTION_SHOW_NOTIFICATION" />
			</intent-filter>
		</receiver>
	</application>
</manifest>